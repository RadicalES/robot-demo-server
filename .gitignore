node_modules/
dist/
dist-bundle.tar.gz
# Cards enrolled while demonstrating. Whoever is showing the terminals should
# not inherit the cards from whoever showed them last.
test-server/cards.json

# Not tracked: this is a demonstration installed fresh, and a lockfile
# committed here pins versions nobody chose on purpose.
package-lock.json

# Python leaves these beside the scale emulator.
__pycache__/
*.pyc

# Internal: stages a bundle into the package repository and publishes it to
# the CDN. This repository is what a customer copies to write their own app -
# our publishing route is not part of that.
stage2cdn.sh
